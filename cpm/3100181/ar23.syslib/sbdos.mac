;
;  BDOS --
;	STANDARD ROUTINE FOR MAKING CALLS TO THE CP/M BDOS
;	DO NOT AFFECT REGISTERS HL AND DE
;
BDOS::
	PUSH	H	; SAVE HL
	PUSH	D	; SAVE DE
	CALL	ENTBDOS	; CALL AT ENTRY POINT
	POP	D	; RESTORE DE
	POP	H	; RESTORE HL
	RET

ENTBDOS	EQU	5	; ENTRY POINT TO CP/M

	END
