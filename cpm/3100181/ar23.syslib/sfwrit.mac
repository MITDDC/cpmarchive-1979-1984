;
;  F$WRITE -- WRITE BLOCK TO FILE WHOSE FCB IS PTED TO BY DE
;	RET W/A=0 IF OK
;
	EXT	BDOS

F$WRITE::
	PUSH	B	; SAVE BC
	MVI	C,B$RECW
	CALL	BDOS
	POP	B	; RESTORE BC
	RET

B$RECW	EQU	21	; WRITE RECORD

	END
