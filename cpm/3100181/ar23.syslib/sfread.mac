;
;  F$READ -- READ BLOCK FROM FILE WHOSE FCB IS PTED TO BY DE
;	RET W/A=0 IF OK
;
	EXT	BDOS

F$READ::
	PUSH	B	; SAVE BC
	MVI	C,B$RECR
	CALL	BDOS
	POP	B	; RESTORE BC
	RET

B$RECR	EQU	20	; READ RECORD

	END
