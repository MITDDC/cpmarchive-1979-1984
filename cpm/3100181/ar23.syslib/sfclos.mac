;
;  F$CLOSE -- CLOSE FILE WHOSE FCB IS PTED TO BY DE
;	RET W/A=0 IF OK
;
	EXT	BDOS

F$CLOSE::
	PUSH	B	; SAVE BC
	MVI	C,B$CLOS
	CALL	BDOS
	CPI	255	; ERROR?
	JZ	FCLSDN
	XRA	A	; A=0
FCLSDN:	POP	B	; RESTORE BC
	RET

B$CLOS	EQU	16	; CLOSE FILE

	END
